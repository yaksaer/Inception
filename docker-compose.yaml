version: '3.7'
networks:
  kseed_net:
    driver: bridge

services: 
  nginx:
    build:
      context: ./srcs/nginx/
      dockerfile: Dockerfile
    volumes: 
      - wordpress:/var/www/html
    ports:
      - 443:443
    container_name: nginx
    networks: 
      - kseed_net
  mysql:
    image: mariadb
    build:
      context: ./srcs/mysql
      dockerfile: Dockerfile
    volumes: 
      - mysql:/var/lib/mysql
    env_file: .env
    container_name: mariadb
    expose: 
      - "3306"
    networks: 
      - kseed_net
  wordpress:
    image: wordpress
    build: 
      context: ./srcs/wordpress
      dockerfile: Dockerfile
    volumes: 
      - wordpress:/var/www/html
    env_file: .env
    depends_on: 
      - mysql
    expose:
      - "9000"
    networks: 
      - kseed_net
     
volumes:
  wordpress:
    driver: local
    driver_opts: 
      type: none
      o: bind
      device: /home/kseed/data/wp
  mysql:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/kseed/data/db
